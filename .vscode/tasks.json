{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build zig test",
            "type": "shell",
            "command": "zig test --test-no-exec -femit-bin=zig-out/bin/${fileBasenameNoExtension}test ${relativeFile}"
        },
        {
            "label": "build zig",
            "type": "shell",
            "command": "zig build install"
        }
    ]
}